# Supabase PostgreSQL — Settings → Database → Connection string
DATABASE_URL="postgresql://user:password@host:6543/postgres?pgbouncer=true"

# Genera con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="cambia-esto-por-una-clave-super-segura-en-produccion"

PORT=3001

# Acepta múltiples orígenes separados por coma:
#   FRONTEND_URL="https://miapp.netlify.app,http://localhost:5173"
FRONTEND_URL="http://localhost:5173"

FOOTBALL_API_KEY="tu-api-key-de-football-data.org"
